# AI-inference-workload-insights
This repository calculates the resource utilization at each layer. It includes at the level of instruction level and even the memory level insights. It is a work in progress.
